image:
  tag: latest
  pullPolicy: Always

serviceAccount:
  annotations:
    # TODO: Create roles for each environment
    eks.amazonaws.com/role-arn: arn:aws:iam::767224633206:role/inspector-pod-test

deployableNamespaces:
  - whoami
  - whoami-dev
